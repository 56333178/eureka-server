server:
  port: 8888
eureka:
  instance:
    hostname: aitieta.com
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://root:123456@aitieta.com:8887/eureka/
spring:
  application:
    name: register-center
  security:
    user:
      name: root
      password: 123456
